/**
 * @deprecated Test file for common/llm.ts which has been removed.
 * The functionality migrated to common/atomicity.ts (sub-agent-based).
 * See tests/common/atomicity.test.ts for the replacement tests.
 */
import { describe, it, expect } from "vitest";

describe("llm module removed", () => {
  it("should have been replaced by atomicity sub-agent", () => {
    expect(true).toBe(true);
  });
});
