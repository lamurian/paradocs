# Types — Type Declarations

Type stubs for npm packages that lack their own types.

| File              | Purpose                                       |
|-------------------|-----------------------------------------------|
| `citation-js.d.ts`| Type stubs for `@citation-js/core` plugins   |

## Conventions

- Ambient module declarations (declare module '...').
- Minimal types — only what extensions actually use.
